# Build artifacts
build/
*.dylib
*.o
*.a
.build/

# Python
__pycache__/
*.pyc
.env*

# macOS
.DS_Store
._*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
xcuserdata/

# Xcode build artifacts
DerivedData/
patcher/build/
patcher/SpliceKit.xcodeproj/xcuserdata/
patcher/SpliceKit.xcodeproj/project.xcworkspace/xcuserdata/

# Pre-built patcher app (generate via release.sh, distribute via GitHub Releases)
patcher/SpliceKitPatcher.app/
patcher/Frameworks/Sentry.framework/
patcher/Frameworks/.sentry-sdk-version

# Keys and credentials
*.pem
*.key
*.p12
*.pfx
*.jks
.credentials
credentials.json

# Swift packages
Package.resolved

# Release binaries (use GitHub Releases instead)
patcher/SpliceKitPatcher-v*.zip
patcher/SpliceKit-v*.dmg

# Runtime metadata export (75MB+, regenerate with tools/fcp_runtime_export.py)
ida_export/

# Decompilation analysis output (multi-GB, local scratch only)
analysis/

# Local scratch/test binaries and artifacts
out/

# Local dev artifacts (not part of any release)
.playwright-mcp/
edit-helper.yml

# Claude Code per-project memory (never commit)
.claude/

# Codex per-project config (never commit)
.codex/
